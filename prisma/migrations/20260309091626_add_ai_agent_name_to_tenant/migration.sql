-- This migration was never applied (applied_steps_count=0).
-- The column was added by 20260309173455_add_ai_agent_name_to_tenant instead.
-- Converted to no-op to avoid duplicate column error in shadow database.
SELECT 1;
